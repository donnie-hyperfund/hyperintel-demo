{
	"name": "services",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"test": "vitest",
		"typegen": "wrangler types --include-runtime=false --strict-vars=false"
	},
	"dependencies": {},
	"devDependencies": {}
}
