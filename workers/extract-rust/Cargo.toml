[package]
name = "hi-extract-rust"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
worker = { version = "0.7", features = ["http"] }
worker-macros = { version = "0.7", features = ["http"] }
http = "1.3"
futures-util = "0.3"

# Vendored extraction crates (will be patched for WASM)
# docx-parser = { path = "vendor/docx-parser" }
# pptx-to-md = { path = "vendor/pptx-to-md" }
