[package]
name = "extract-tester"
version = "0.0.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
markdownify = { path = "../vendor/markdownify" }
wasm-bindgen = "0.2"
