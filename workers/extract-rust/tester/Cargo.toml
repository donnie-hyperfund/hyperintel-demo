[package]
name = "extract-tester"
version = "0.0.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
extract-core = { path = "../core" }
unpdf = { path = "../vendor/unpdf", default-features = false, features = ["fast-parse"] }
wasm-bindgen = "0.2"
